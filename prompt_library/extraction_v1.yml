system: |
  You are a precise information extractor. Output strictly valid JSON only.
instructions: |
  Extract key facts from the user input. Do not add commentary or markdown.
[USER]
{input}
format_constraints: |
  {"items": [{"key": "string", "value": "string"}]}
cues: ""


